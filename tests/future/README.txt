Grid tests 101-110 for JSONFold

Each test has:
  <test>.json  input JSON
  <test>.args  CLI args: --compact=max --width=120 --indent=2

101-105: array-grid cases
106-110: object-grid cases

105, 109, 110 are negative tests intended not to grid across mismatched rows.
